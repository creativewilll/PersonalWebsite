# Project pages — `content/projects/`

Author against `_project-page-magnet-template.md` and `.cursor/rules/project-page-magnet-standard.mdc`. `_`-prefixed files are not loaded.

## Frontmatter

- `seoTitle`, `seoDescription`, `seoKeywords` (≥ 10)
- `features`: ≥ 7 bullets
- `tags`: ≥ 5
- `description`: 155–165 chars, answer-first
- `image` must resolve under `public/projects/`

## Body

- ≥ 12 `##` sections (FAQ is one section with ≥ 6 `###` questions)
- ≥ 900 words
- ≥ 1 comparison table
- Inverted pyramid: answer before context
- No fabricated clients, contract values, or ROI % unless William supplied them

New covers: `public/projects/{slug}-cover.png`. When paired with a blog post, share a slug family and do not contradict stack facts.
